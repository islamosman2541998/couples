# السلم والتعبان للزوجين

التثبيت على المشروع الحالي يحافظ على بيانات الألعاب الأخرى:

```sh
php artisan migrate --force
php artisan db:seed --class=SnakesGameSeeder --force
npm run build
```

اللعب: `/games/snakes-and-ladders/play`، وإدارة الخانات: `/admin/snake-cells`.
اللعبة مجانية افتراضيًا، والسعر والتفعيل متاحان من بيانات الألعاب. التثبيت من
الصفر عبر `DatabaseSeeder` يضيفها أيضًا. إعادة Seeder لا تستبدل تعديلات المدير.

١٠٠ خانة بمحتوى رومانسي غير صريح، ٦ سلالم و٦ ثعابين محددة في `config/snakes.php`.
الحركة تبدأ من خارج اللوحة، أسفل اليمين، في مسار متعرج. يتبع اللاعب مسارًا واحدًا
لكل رمية، ثم ينفذ تحدي خانة الوصول أو يتخطاه بلا عقوبة. لا دور إضافي عند رقم ٦،
والوصول إلى ١٠٠ يحتاج رمية مطابقة؛ الزيادة تُبقي اللاعب مكانه ويتبدل الدور.

الخانات لها أرقام ثابتة؛ تعطيل التحدي يحوّله لاستراحة ولا يحذف الخانة أو يغيّر المسار.
يُحفظ تقدم الجولة والأسماء والجائزة في localStorage، مع تثبيت نتيجة الرمية قبل
الأنيميشن لمنع إعادة الرمي عند تحديث الصفحة. تتجاهل اللعبة الحفظ غير الصالح أو
القديم عند تغيير بيانات اللوحة، وتظل قابلة للعب إذا كان التخزين محظورًا.

```sh
php artisan test
node --test tests/game-engines.test.mjs tests/control-game.test.mjs tests/snakes-game.test.mjs
```
