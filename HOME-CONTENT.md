# محتوى الصفحة الرئيسية

من لوحة التحكم افتح **محتوى الرئيسية** (`/admin/home-content`).

- السلايدر: إضافة وتعديل وحذف وإخفاء وترتيب الشرائح، رفع صورة أساسية وصورة اختيارية للموبايل، رابط لكل شريحة، ضبط التشغيل التلقائي والسرعة والأسهم والنقاط. المقاس المقترح 900 × 470. الصور المقبولة JPG وPNG وWebP حتى 5MB.
- آراء العملاء: اسم العميل وصورة المحادثة مع إمكانية التكبير. القسم لا يظهر قبل إضافة رأي ظاهر. أخفِ البيانات الخاصة من المحادثة قبل رفعها.
- العرض: عداد 12 ساعة افتراضياً، محفوظ في متصفح الزائر، أو موعد انتهاء ثابت وفق المنطقة الزمنية المكتوبة بجوار الحقل. يختفي عند انتهائه. خيار بدء مدة جديدة يعيد تشغيله لجميع الزوار عند زيارتهم التالية. لا يغير أسعار الألعاب تلقائياً.
- الإشعارات: 100 اسم تجريبي، ظهور 4 ثوانٍ وفاصل 10 ثوانٍ افتراضياً. يمكن ضبط الأسماء والألعاب والمكان والمدد والإيقاف. تحمل علامة «نشاط ترويجي» ولا تسحب طلبات العملاء الحقيقية. زر الإغلاق يخفيها لباقي جلسة المتصفح.

الإعدادات محفوظة في جدول `settings` الحالي بمفاتيح `home_settings` و`home_slides` و`home_reviews`. لا توجد migrations جديدة. الصورة المرفقة موجودة في `public/images/home/couples-banner.jpg`، والصور المرفوعة في قرص `public` داخل `homepage`.

عند النقل لخادم آخر: انقل ملفات المشروع والصورة، شغّل `npm ci` ثم `npm run build` أو ارفع مجلد `public/build` المبني، وتأكد من تشغيل `php artisan storage:link` وإمكانية الكتابة داخل `storage`. يمكن مسح قوالب Blade القديمة بـ `php artisan view:clear`.

التحقق: `php artisan test --filter=HomeContentTest`.
