# تقرير فحص الموقع — 5 سبتمبر 2026

## تحديث المحتوى الرومانسي

استُبدل محتوى الألعاب المحلية بناءً على طلب المالك بمكتبة جديدة من 153 عنصرًا: 45 كارتًا موزعة بالتساوي على «شرارة البداية / أقرب شوية / جرأة القلوب»، و12 نتيجة للسبينر، و24 كارت خربشة، و24 سؤال «مين فيكم» بتحديات مصاحبة، و24 تحديًا، و24 سؤال معرفة بالشريك. أزيلت مراجع الصور القديمة من عناصر اللعب المستبدلة؛ احتُفظ بالملفات القديمة لتبقى النسخة الاحتياطية قابلة للاسترجاع.

المصدر القابل للتعديل: `database/seeders/data/romantic-content.php`. الحسابات والاشتراكات وأسماء الألعاب وروابطها وأسعارها لم تتغير. أضيفت أزرار لتخطي الكروت والأسئلة وتوضيح أن التقارب باختيار الطرفين.

النسخة الاحتياطية الخاصة قبل الاستبدال: `storage/app/backups/game-content/20260905-163734-4356260c-ff9c-41de-8045-52cdaa1c2813.json`.

فحص التحديث: 39 اختبار Laravel ناجحًا (305 تحققات)، و7 اختبارات JavaScript ناجحة، وبناء ناجح. فُتحت الألعاب الستة بعد الاستبدال، وظهر المحتوى الجديد دون أخطاء JavaScript مسجلة في هذه الزيارات. أمر `games:refresh-content --replace` ينشئ نسخة احتياطية ويتحقق منها قبل استبدال جداول محتوى اللعب داخل معاملة واحدة؛ تشغيله مجددًا سيستبدل أي تعديلات محتوى لاحقة.

## نتيجة الفحص الأصلي

الفحص أُجري على النسخة المحلية `http://couples-game.test`، مع إصلاح المشاكل المثبتة. النتيجة: 37 اختبار Laravel ناجحًا (252 تحققًا)، و6 اختبارات JavaScript ناجحة، وبناء الواجهة ناجح. هذه نتيجة للسيناريوهات المختبرة، وليست ضمانًا بعدم وجود أي عيب في كل الظروف.

## الألعاب

| اللعبة | التجربة الفعلية في المتصفح | الإصلاحات والتحقق الآلي |
|---|---|---|
| الكروت | تحميل وكشف كارت من المستويات الثلاثة | حماية API باشتراك اللعبة المحددة، مطابقة اللاعب مع المستهدف بالكارت، معالجة فشل التحميل ومنع طلبات متزامنة، منع اختيار مستوى فارغ، حماية أسماء المستويات التي تحتوي علامات اقتباس |
| السبينر | دوران وظهور نتيجة | استمرار الدوران للأمام، 30 لفة متتالية مع مطابقة القطاع للنتيجة، منع الدوران المكرر أثناء الحركة، إغلاق الصوت بعد انتهائه |
| خربش والعب | السحب لكشف كارت واحد، كشف الكل ثم إعادة اللعب | تحديث حالة كل كارت، إعادة التغطية دون إعادة تحميل، إخفاء أحكام الكروت غير المكشوفة، إلغاء أثر حركة الكشف عند إعادة اللعب |
| بوس أو دوس | اختيار الراجل «أنا» والست «شريكي» واحتساب نقطة اتفاق | مقارنة الشخص المقصود بدل النص، اختبار التركيبات الأربع، منع تكرار احتساب النتيجة |
| شوق أو دوق | كشف تحدٍ، تشغيل وانتهاء المؤقت، الوصول لشاشة النتيجة | إصلاح زر الكارت الأخير الذي كان يمنع النهاية، إصلاح إجمالي الكروت والتقدم، إيقاف المؤقت عند الرجوع وإعادة اللعب، منع تكرار المؤقت والتنقل |
| المخطوبين | بدء جولة، اختيار إجابة وتخمينها وعرض النتيجة | اختبار الفئات وتبادل اللاعبين والنهاية وإعادة اللعب، منع تكرار النتيجة والإجابات الفارغة |

تم فتح الألعاب الستة بمقاس شاشة موبايل 390×844؛ لم يظهر تجاوز أفقي أو أخطاء JavaScript مسجلة في هذه الزيارات. السحب جُرّب باستخدام المؤشر على هذا المقاس، وليس جهاز لمس فعليًا.

## الحسابات والإدارة والاشتراكات

- عزل اختبارات قاعدة البيانات باستخدام SQLite في الذاكرة؛ لم تُشغّل عمليات إعادة إنشاء الجداول على قاعدة الموقع.
- تحديث الاختبارات القديمة لمسارات الحساب الحالية، وإصلاح روابط تأكيد البريد وكلمة المرور التي كانت تشير إلى dashboard غير موجود.
- إضافة قيم افتراضية صحيحة لحالة المستخدم، وإبطال توثيق البريد عند تغييره، وإبطال الجلسة للحساب المعطّل.
- اختبار الصفحات العامة والحساب ولوحة الإدارة وصفحات إنشاء المحتوى، وصلاحيات الزائر والمستخدم العادي والمدير.
- اختبار إنشاء وتعديل وحذف محتوى الألعاب داخل قاعدة الاختبار، وإصلاح تجاهل إلغاء «مفعّل» والقيم الاختيارية الفارغة.
- منع إضافة سؤال اختيارات بأقل من اختيارين صالحين، مع الحفاظ على الاختيار النصي «0».
- معالجة تكرار اسم اللعبة دون خطأ في الرابط الفريد، والحفاظ على رابط اللعبة عند تعديل اسمها.
- إصلاح إظهار سعر اللعبة عند إلغاء اختيار المجانية في نموذج الإنشاء.
- اشتراط تسجيل الدخول لطلب الاشتراك حتى يرتبط الطلب بحساب؛ رفض الاشتراك في لعبة مجانية أو معطّلة، وتحديث تفاصيل السعر عند تغيير اللعبة.
- اختبار رفض الاشتراك المنتهي أو المعلّق واشتراك لعبة أخرى، ومنع إظهار المحتوى المعطّل.
- حفظ الإيصالات الجديدة في التخزين الخاص، وعرضها عبر مسار محمي للمدير. نُقل الإيصال الموجود محليًا من `storage/app/public/receipts` إلى `storage/app/receipts` دون تغيير اسمه أو محتواه. الاختبارات تؤكد أن الملف الجديد غير موجود في القرص العام وأن الزائر والمستخدم العادي لا يستطيعان فتح مسار الإيصال.
- إزالة مسارات عرض إدارية كانت تشير إلى دوال غير موجودة.

## إعادة الفحص

```sh
php artisan test --compact
node --test tests/game-engines.test.mjs
npm run build
git diff --check
```

## حدود الفحص وملاحظات التشغيل

- لم يتم نشر تغييرات على استضافة خارجية أو اختبار إعدادات السيرفر المنشور وHTTPS والأحمال أو متصفحات وأجهزة حقيقية متعددة.
- إرسال البريد اختُبر داخل بيئة اختبار؛ وصول رسائل إعادة تعيين كلمة المرور إلى بريد حقيقي يحتاج اختبار SMTP الفعلي.
- الاشتراك يعتمد على رفع إيصال ومراجعة إدارية، وليس تكاملًا بنكيًا للتحقق من التحويل. لم يُنفّذ تحويل مالي حقيقي.
- بيانات التواصل الظاهرة لا تزال تجريبية، مثل `info@example.com` ورقم `0500000000`؛ تحتاج بيانات المالك الفعلية قبل النشر.
- بناء الواجهة يعرض تنبيهًا غير مانع عن قِدم بيانات Browserslist.
- عند نقل الكود إلى خادم آخر، يجب نقل إيصالات ذلك الخادم من مجلد `storage/app/public/receipts` إلى `storage/app/receipts` مع الحفاظ على الأسماء ودون الكتابة فوق ملفات موجودة؛ نقل الملفات هنا تم على البيئة المحلية فقط.
- الفحص تضمن صلاحيات وخصوصية وحالات تشغيل محددة، ولم يتضمن تدقيق اختراق كاملًا أو فحص جميع الثغرات المعروفة في التبعيات.
