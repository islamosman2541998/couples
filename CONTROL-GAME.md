# لعبة السيطرة

تثبيت اللعبة على قاعدة بيانات موجودة، مع الاحتفاظ بالألعاب والمستخدمين الحاليين:

```sh
php artisan migrate --force
php artisan db:seed --class=ControlGameSeeder --force
npm run build
```

اللعبة: `/games/control-game/play`، والكروت: `/admin/control-cards`.
تُضاف اللعبة مجانية افتراضيًا ويمكن تغيير سعرها وتفعيلها من إدارة الألعاب.
التثبيت يضيف ٥٠ كارتًا غير صريح، ويمكن تكراره دون تعديل النصوص أو الأسعار المحفوظة.
تثبيت المشروع من الصفر عبر `DatabaseSeeder` يشمل اللعبة تلقائيًا.

كل تنفيذ يضيف نقطتين. أول رفض لكل لاعب مجاني وبعده يخصم الرفض نقطتين.
الأدوار تتبادل والكروت تُخلط دون تكرار خلال الجولة. العودة للقواعد تحفظ الجولة
داخل الصفحة، بينما تحديث الصفحة يبدأ من جديد. الصور اختيارية وتُرفع من لوحة الإدارة.

```sh
php artisan test
node --test tests/game-engines.test.mjs tests/control-game.test.mjs
```
