# إظهار الألعاب الجديدة على السيرفر

رفع ملفات PHP لا ينقل بيانات قاعدة البيانات المحلية. لعبة السيطرة والسلم والتعبان تحتاجان سجلات ألعاب وجداول ومحتوى.

## بدون Terminal

ارفع ملفات التحديث، ثم افتح **لوحة التحكم ← إدارة الألعاب ← تثبيت الألعاب الجديدة**. ينتظر الزر اكتمال تجهيز الجداول الناقصة وإضافة اللعبتين و50 كارت سيطرة و100 خانة للسلم والتعبان. الألعاب الجديدة تكون مفعلة ومجانية، ويمكن تغيير سعرها من إدارة الألعاب.

التثبيت يتطلب حساب مدير. يمكن تكراره؛ لا يمسح البيانات ولا يستبدل الأسعار أو الصور أو المحتوى المعدل أو حالة تفعيل الألعاب الموجودة. إذا كانت لعبة موجودة لكنها مخفية، فعّلها من إدارة الألعاب.

## باستخدام Terminal

من مجلد المشروع على السيرفر:

```sh
php artisan games:install-new
```

إذا كان السيرفر يستخدم كاش للمسارات أو القوالب، بعد رفع الملفات:

```sh
php artisan route:clear
php artisan view:clear
```

## الملفات المطلوبة

- `app/Support/InstallNewGames.php`
- `app/Console/Commands/InstallNewGamesCommand.php`
- `app/Http/Controllers/Admin/GameManageController.php`
- `routes/web.php`
- `resources/views/admin/games/index.blade.php`
- ملفات اللعبتين المرفوعة سابقاً: Models وViews وJS وCSS وconfig، والـ migrations والـ seeders وملفات `database/seeders/data`.
- مجلد `public/build` المبني بعد التحديث.

إذا فشل التثبيت، راجع `storage/logs/laravel.log` وصلاحيات إنشاء الجداول. بعد تصحيح السبب يمكن إعادة المحاولة. لا تستخدم `migrate:fresh` أو `db:seed` العام على بيانات الموقع الحالي.
